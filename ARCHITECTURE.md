# Архитектура приложения

## Общая схема (MVVM)

```
┌─────────────────────────────────────────────────────────────┐
│                         VIEW (XAML)                         │
│  ┌───────────────────────────────────────────────────────┐  │
│  │           MainWindow.xaml                             │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐  │  │
│  │  │  Settings   │  │   Actions    │  │  Statistics │  │  │
│  │  │   Panel     │  │    Panel     │  │    Panel    │  │  │
│  │  └─────────────┘  └──────────────┘  └─────────────┘  │  │
│  │  ┌───────────────────────────────────────────────────┐  │
│  │  │          Results DataGrid                         │  │
│  │  └───────────────────────────────────────────────────┘  │
│  └───────────────────────────────────────────────────────┘  │
│                            ▲                                │
│                            │ Data Binding                   │
│                            │                                │
└────────────────────────────┼────────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────────┐
│                     VIEWMODEL                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           MainViewModel                              │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │  Observable Properties:                        │  │   │
│  │  │  • XmlFilePath                                │  │   │
│  │  │  • DirectoryPath                              │  │   │
│  │  │  • CheckResults (ObservableCollection)        │  │   │
│  │  │  • Statistics (Counts)                        │  │   │
│  │  │  • StatusMessage                              │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │  Commands (RelayCommand):                      │  │   │
│  │  │  • SelectXmlFileCommand                       │  │   │
│  │  │  • SelectDirectoryCommand                     │  │   │
│  │  │  • CheckFilesCommand                          │  │   │
│  │  │  • ExportResultsCommand                       │  │   │
│  │  │  • ClearResultsCommand                        │  │   │
│  │  │  • OpenFileLocationCommand                    │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
│                            ▲                                │
│                            │ Uses Services                  │
└────────────────────────────┼────────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────────┐
│                       SERVICES                              │
│  ┌─────────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ XmlParserService│  │FileCheckServ │  │ Crc32Service │   │
│  ├─────────────────┤  ├──────────────┤  ├──────────────┤   │
│  │ • ParseXmlFile()│  │ • CheckFiles │  │ • Calculate  │   │
│  │                 │  │ • CheckFile  │  │   Checksum() │   │
│  │                 │  │ • CheckSign  │  │ • Compare    │   │
│  │                 │  │ • FindFile   │  │   Checksums()│   │
│  └─────────────────┘  └──────────────┘  └──────────────┘   │
│           │                   │                   │         │
│           └───────────────────┴───────────────────┘         │
│                               ▼                             │
└───────────────────────────────┼─────────────────────────────┘
                                │
┌───────────────────────────────┼─────────────────────────────┐
│                          MODELS                             │
│  ┌──────────────┐  ┌────────────────┐  ┌────────────────┐  │
│  │  Document    │  │ FileCheckResult│  │   FileInfo     │  │
│  ├──────────────┤  ├────────────────┤  ├────────────────┤  │
│  │ • DocType    │  │ • FileName     │  │ • FileName     │  │
│  │ • DocName    │  │ • Status       │  │ • FileFormat   │  │
│  │ • DocNumber  │  │ • Message      │  │ • FileChecksum │  │
│  │ • DocDate    │  │ • FilePath     │  │ • SignFile     │  │
│  │ • Files[]    │  │ • Checksums    │  └────────────────┘  │
│  └──────────────┘  └────────────────┘                       │
└─────────────────────────────────────────────────────────────┘
```

## Поток данных при проверке файлов

```
┌──────────────┐
│   User       │ 1. Clicks "Выполнить проверку"
│   Action     │─────────────────────────────────┐
└──────────────┘                                 │
                                                 ▼
┌─────────────────────────────────────────────────────────┐
│  MainViewModel.CheckFilesCommand                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │ 1. Validation (XML & Directory exist)             │  │
│  │ 2. Set IsChecking = true                          │  │
│  │ 3. Call Services in background Task               │  │
│  └───────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  XmlParserService.ParseXmlFile()                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │ • Read XML file                                   │  │
│  │ • Parse <Document> elements                       │  │
│  │ • Extract <File> and <SignFile> information       │  │
│  │ • Return List<Document>                           │  │
│  └───────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  FileCheckService.CheckFiles()                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │ For each file in documents:                       │  │
│  │   ├─> CheckFile()                                │  │
│  │   │    ├─> FindFileByName()                      │  │
│  │   │    ├─> FindFileByChecksum() (if not found)   │  │
│  │   │    └─> CheckSignature()                      │  │
│  │   │         ├─> FindSignatureByName()            │  │
│  │   │         └─> FindSignatureByChecksum()        │  │
│  │   └─> Create FileCheckResult                     │  │
│  │                                                   │  │
│  │ CheckUnprocessedFiles():                          │  │
│  │   └─> Find files not in XML with .sig files      │  │
│  │                                                   │  │
│  │ Return List<FileCheckResult>                      │  │
│  └───────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  Crc32Service.CalculateChecksum()                       │
│  ┌───────────────────────────────────────────────────┐  │
│  │ • Read file in chunks                             │  │
│  │ • Calculate CRC32 using polynomial 0xEDB88320     │  │
│  │ • Return checksum as HEX string                   │  │
│  └───────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  MainViewModel - Update UI                              │
│  ┌───────────────────────────────────────────────────┐  │
│  │ • Add results to CheckResults collection          │  │
│  │ • Calculate statistics (counts)                   │  │
│  │ • Update StatusMessage                            │  │
│  │ • Set IsChecking = false                          │  │
│  └───────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  View Updates (via Data Binding)                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │ • DataGrid displays results                       │  │
│  │ • Statistics panel updates                        │  │
│  │ • Status bar shows completion message             │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## Логика проверки файла (детально)

```
CheckFile(xmlFile)
    │
    ├─> 1. Поиск по имени
    │   │
    │   ├─> Найден?
    │   │   ├─> ДА: Сравнить контрольные суммы
    │   │   │   ├─> Совпадают? ✅ SUCCESS
    │   │   │   └─> Не совпадают? ⚠️ WARNING
    │   │   │
    │   │   └─> НЕТ: ⬇️
    │   │
    │   └─> 2. Поиск по контрольной сумме
    │       │
    │       ├─> Найден?
    │       │   └─> ДА: ⚠️ WARNING (имя не совпадает)
    │       │
    │       └─> НЕТ: ❌ ERROR (файл не найден)
    │
    └─> 3. Проверка подписи (если есть <SignFile>)
        │
        ├─> 3.1. Поиск sig по имени
        │   │
        │   ├─> Найден?
        │   │   ├─> ДА: Сравнить контрольные суммы
        │   │   │   ├─> Совпадают? ✅ добавить к сообщению
        │   │   │   └─> Не совпадают? ⚠️ обновить статус
        │   │   │
        │   │   └─> НЕТ: ⬇️
        │   │
        │   └─> 3.2. Поиск sig по контрольной сумме
        │       │
        │       ├─> Найден?
        │       │   └─> ДА: ⚠️ WARNING (имя подписи не совпадает)
        │       │
        │       └─> НЕТ: ❌ ERROR (подпись не найдена)
        │
        └─> Вернуть FileCheckResult
```

## Зависимости между компонентами

```
MainViewModel
    │
    ├──> XmlParserService
    │       └──> Document (Model)
    │
    ├──> FileCheckService
    │       ├──> Crc32Service
    │       ├──> FileCheckResult (Model)
    │       └──> FileInfo (Model)
    │
    └──> FileCheckResult (ObservableCollection)

MainWindow (View)
    │
    ├──> MainViewModel (DataContext)
    │
    └──> Converters
            ├──> StatusToColorConverter
            ├──> StatusToIconConverter
            ├──> InverseBooleanConverter
            └──> ZeroToVisibilityConverter
```

## Паттерны проектирования

### 1. MVVM (Model-View-ViewModel)
- **Model**: Document, FileInfo, FileCheckResult
- **View**: MainWindow.xaml
- **ViewModel**: MainViewModel

### 2. Service Layer
- XmlParserService
- FileCheckService
- Crc32Service

### 3. Command Pattern
- RelayCommand (из CommunityToolkit.Mvvm)
- Привязка команд к кнопкам

### 4. Observable Pattern
- ObservableObject
- ObservableProperty
- ObservableCollection

### 5. Dependency Injection (Manual)
- Services создаются в ViewModel
- Передаются через конструктор

## Файловая структура

```
FileSignatureChecker/
│
├── 📄 FileSignatureChecker.csproj    # Конфигурация проекта
├── 📄 App.xaml                       # Конфигурация приложения
├── 📄 App.xaml.cs                    # Code-behind
├── 📄 MainWindow.xaml                # Главное окно (View)
├── 📄 MainWindow.xaml.cs             # Code-behind
│
├── 📁 Models/                        # Модели данных
│   ├── Document.cs                   # Модель документа из XML
│   └── FileCheckResult.cs            # Модель результата проверки
│
├── 📁 ViewModels/                    # Логика представления
│   └── MainViewModel.cs              # ViewModel главного окна
│
├── 📁 Services/                      # Бизнес-логика
│   ├── XmlParserService.cs           # Парсинг XML
│   ├── FileCheckService.cs           # Проверка файлов
│   └── Crc32Service.cs               # Вычисление контрольных сумм
│
├── 📁 Converters/                    # Конвертеры для XAML
│   ├── StatusToColorConverter.cs     # Статус → Цвет
│   ├── StatusToIconConverter.cs      # Статус → Иконка
│   ├── InverseBooleanConverter.cs    # Bool → !Bool
│   └── ZeroToVisibilityConverter.cs  # 0 → Visible
│
└── 📁 Documentation/
    ├── README.md                     # Основная документация
    ├── QUICKSTART.md                 # Быстрый старт
    ├── INSTALLATION.md               # Инструкция по установке
    └── UI_DESCRIPTION.md             # Описание интерфейса
```